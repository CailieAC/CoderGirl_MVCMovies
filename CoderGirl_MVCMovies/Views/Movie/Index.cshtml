@model IEnumerable<CoderGirl_MVCMovies.ViewModels.Movies.MovieListItemViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                Director
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th>Average Rating</th>
            <th>Number of Ratings</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr id="@Html.DisplayFor(modelItem => item.Id)">
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirectorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            @*@if (movie.Ratings.Count > 0)
            {
                <td>@movie.Ratings.Average()</td>
            }
            else
            {
                <td>none</td>
            }
            <td>@movie.Ratings.Count</td>*@
            <td>
                @*TODO: change below to averagerating *@
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @*TODO: change below to number of rating *@
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })  |
                @* make below link go to movierating controller*@
                @Html.ActionLink("Rate Movie", "Create", "MovieRating", new { movieId = item.Id }, null)
                @*<a asp-controller="MovieRating" asp-action="Create" asp-route-movieId="@Model.Id">Rate</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
